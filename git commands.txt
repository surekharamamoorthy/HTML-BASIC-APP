before doing doing anything add below lines in git bash
    git config --global user.name "surekha"
    git config --global user.email "purushoth231988@gmail.com"
in git bash - cd
cd /D/html-basic-app
git init - to initialize empty repository in git
git add . - add all the files to git
git status - to check the status of the files
git commit -am - to commit the changes
git remote add origin https://github.com/surekharamamoorthy/html-basic-app.git - add local to remote
git push origin master - push the code to master branch
